# --- Web Framework & Server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Database & Migrations ---
sqlalchemy==2.0.35
alembic==1.13.2
pydantic-settings==2.5.2

# --- Security & Auth ---
bcrypt==4.0.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0

# --- Computer Vision & AI ---
ultralytics==8.3.0
opencv-python-headless==4.9.0.80            # Linux 伺服器用 headless，Windows 你可以用一般的 opencv-python
numpy<2.0.0,>=1.23                           # 明確版本區間，避免裝到 2.x

# --- Torch（建議指定版本，避免每次隨便抓一個） ---
# CPU 版本範例（可依你實際環境調整）
torch==2.11.0+cpu
torchvision==0.19.1+cpu

# --- Utilities ---
python-dotenv==1.0.0
psutil==5.9.8

# --- 其他可選（如果你有空間、CPU 跑得動） ---
mediapipe==0.10.33                           # 你已經成功安裝了
# 之後如果要再用 torchreid，也可以加（但實際上是 GitHub 安裝，不一定要寫進 requirements）
